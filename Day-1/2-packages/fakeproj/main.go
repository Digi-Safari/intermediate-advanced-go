package fakeproj

// if you have multiple exec, then you can put them in one single place in cmd folder

// internal folder restricts access
// no one outside the current project can access the internal folder

// package names should be simple
// don't use dashes or underscore

// don't use util/helper packages in your project

func main() {
	//ioutil.ReadFile()
	//os.ReadFile()
	//ioutil.ReadAll()
	//io.ReadAll()
}
