package main

func main() {
	// if we don't provide any values to the variable,
	//they get initialized with there default values

	// putting related things together
	var (
		name  = "ajay"
		age   = 30
		marks = 80.5
	)
	_, _, _ = name, age, marks

	// peek into it for design pattern
	//os.OpenFile()
	//os.O_APPEND
	//time.Second
	//http.StatusOK
}
